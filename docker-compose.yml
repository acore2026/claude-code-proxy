services:
  proxy:
    build:
      context: .
      dockerfile: Dockerfile
    image: claude-code-proxy:w3
    env_file:
      - .env.w3
    ports:
      - 8082:8082
    volumes:
      - w3_tokens:/data

volumes:
  w3_tokens:
